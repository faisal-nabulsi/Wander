{
  "build": 156,
  "version": "1.0",
  "payloadURL": "https://github.com/faisal-nabulsi/Wander/releases/latest/download/Wander.ipa",
  "notes": "Fixes Apple ID sign-in, which could still fail with a 429 error after the last update. Apple now accepts only two requests per connection, and signing in takes three, so the last step was being refused. Each request to Apple now uses its own connection, and sign-in completes."
}
