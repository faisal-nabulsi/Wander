{
  "build": 79,
  "version": "1.0",
  "payloadURL": "https://wanderspoofer.com/downloads/Wander.ipa",
  "notes": "Update installs are more reliable: the updater now retries once on a transient tunnel hiccup, and if it still can't reach your device it tells you exactly what to do (make sure LocalDevVPN is connected, then teleport once to wake the developer image) instead of a raw 'connection reset' error."
}
